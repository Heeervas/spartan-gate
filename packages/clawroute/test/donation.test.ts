/**
 * Donation endpoints removed — the original donationware model has been stripped.
 */
import { describe, it } from 'vitest';

describe('Billing endpoints', () => {
    it.skip('donation endpoints removed', () => {});
});
